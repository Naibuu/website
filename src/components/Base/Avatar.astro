---
import { Metadata } from '@/consts'

interface Props {
    size: number
}

const { size } = Astro.props
---

<img
    src="/assets/profile.png"
    alt={Metadata.title}
    width={size}
    height={size}
    draggable={false}
    class="rounded-full shrink-0 shadow select-none"
/>
